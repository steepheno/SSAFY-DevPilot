  nginx:
    image: nginx:alpine
    container_name: ##PROJECT_NAME##-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - frontend
      - backend
    networks:
      - ##PROJECT_NAME##-network
    restart: always