nginx:
    image: nginx:alpine
    container_name: damul-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - frontend
      - backend
    networks:
      - damul-network
    restart: always