import { FolderIcon } from 'lucide-react';

const PemUploaderContainer = () => {
  return (
    <div className="flex items-center justify-center rounded-md bg-gray-300 p-2">
      <FolderIcon width={24} height={24} color={'gray'} />
    </div>
  );
};

export default PemUploaderContainer;
